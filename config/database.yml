default: &default
  adapter: postgis
  
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  # ↓↓↓ DATABASE_URLがすべての接続情報を管理します ↓↓↓
  url: <%= ENV.fetch("DATABASE_URL") %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
  cable:
    <<: *default
